{
  "name": "@mine/markdown-loader",
  "version": "0.0.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "lint:es": "eslint --ext .js,.ts,.tsx ./",
    "lint:es-fix": "npm run lint:es -- --fix",
    "build": "rm -rf ./lib && tsc -p ./tsconfig.json",
    "ts:check": "tsc -p ./tsconfig.json --noEmit"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "markdown-toc": "^1.2.0"
  }
}
