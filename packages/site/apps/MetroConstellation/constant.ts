/**
 * 地铁线宽度，需要和 css 中的变量相同且都需要是奇数
 */
export const LINE_WIDTH = 3

/**
 * 未 active 时的颜色，需要和 css 中的变量相同
 */
export const COLOR_BASE = '#bbb'

/**
 * active 时的颜色，需要和 css 中的变量相同
 */
export const COLOR_LINE_LIGHT = '#ddd'
